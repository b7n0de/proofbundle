# Trusted ed25519 public keys (base64, raw 32-byte) that may sign a RELEASE-EVIDENCE artifact —
# the recorded fuzz-soak, the Python<->Rust differential matrix, and every other artifact a
# RELEASE-DECIDING check in scripts/audit_candidate_matrix.py admits.
#
# The gate reads this file from the COMMITTED tree (`git show HEAD:...`), never from the working
# tree: reading the working tree would let a dirty checkout inject a key and self-attest evidence
# (the same reasoning as audit_artifacts/pre_tag_trusted_pubkeys.txt, Spur-2 lens A, 2026-08-27).
#
# The PRIVATE half of each key is a release secret held by the RUNNER (CI / owner), NEVER in this
# repo. An EMPTY file (only comments) = NO trust anchor: every release-deciding artifact read then
# fails closed. That is the state today, on purpose — see below.
#
# WHY THIS FILE IS EMPTY, and why that is the honest state rather than an oversight
# (deep gate 2026-09-05, finding L5-G7-02, P2). Until this file existed, checks C6.2/C6.3/C8.2
# granted PASS from UNSIGNED, version-free JSON: a 3.6.0-scoped soak artifact decided 6.0.0, a
# two-key JSON decided "0 crash, 0 false-accept", and `{"all_agree": true}` decided "Python == Rust
# on all" over zero vectors. Adding the mechanism without a key makes those rows RED, and red is
# what they should have been all along. A key is added here in an owner-approved commit as part of
# a release ceremony, together with re-run, candidate-bound evidence signed by its private half
# (scripts/sign_readiness_artifact.py).
#
# Separate from pre_tag_trusted_pubkeys.txt ON PURPOSE: a key that may attest routine release
# evidence must not thereby be able to sign a pre-tag audit receipt. Two trust anchors, two scopes.

# LINE FORMAT, one key per line (Auflage C3, second half, 2026-09-06):
#
#     <base64 raw 32-byte ed25519 public key> role=<role> not_after=<YYYY-MM-DD>
#
# BOTH FIELDS ARE MANDATORY, and a line missing either is DISCARDED — not read as "a key without
# restrictions". Before this format the anchor was a flat list of bare keys: it said WHO may sign,
# but not WHAT FOR and UNTIL WHEN. `signer_role` in an artifact was therefore checked only for
# PRESENCE — a field the producer writes itself, verified against nothing. A role the audited party
# assigns to itself is not a role.
#
#   role        what this key may speak for. The gate compares it to the artifact's `signer_role`
#               and refuses a mismatch: the ANCHOR decides what a key attests, not the artifact
#               that wants to be admitted. Use one role per purpose (e.g. `release-runner`), never
#               a catch-all — the point of the field is that it can fail to match.
#   not_after   last day this key may produce evidence. Checked against TWO deadlines, and both
#               must hold: the artifact's `produced_at` (evidence produced AFTER the window is
#               inadmissible — it does not become lawful by being read early) AND the day the gate
#               runs (an expired key admits nothing NOW).
#
#               THE SECOND DEADLINE WAS ADDED ON 2026-09-06, and this paragraph said the opposite
#               before: "compared against the artifact's produced_at, not against 'now'. Evidence
#               that was lawful yesterday does not become inadmissible because the matrix runs
#               today." The first half is true; the conclusion is not. `produced_at` is written by
#               the SAME holder that signs, and the signature covers it — so a holder of an expired
#               private half simply writes a date inside the window. MEASURED that day against this
#               exact mechanism with a throwaway keypair: honest produced_at -> untrusted,
#               produced_at backdated into the window -> verified. The reachable corridor was as
#               wide as the evidence freshness window (180 days from the day after expiry).
#               A self-asserted time cannot establish WHEN a signature was made; that is what
#               trusted timestamps (RFC 3161) exist for. Without one, the only sound reading is
#               that the window must be open now, too.
#
#               WHAT IT COSTS, stated plainly: after a key expires, evidence it signed becomes
#               inadmissible here — including evidence that was produced lawfully. That is the same
#               trade as code signing without a timestamp, and it is affordable here because this
#               gate judges a CANDIDATE with fresh measurement (evidence is capped at 180 days by
#               the freshness check either way), not an archive. To keep releasing past a key's
#               window, roll the key — that is the intended move.
#
# The stricter format costs nothing today precisely BECAUSE this file is empty: there is no
# existing entry a tighter reading would break. That is why the format is fixed now — later, every
# tightening would be a break.

# ── 6.0.0 ─────────────────────────────────────────────────────────────────────────────────────
# OWNER-AUTORISIERUNG: Auftrag 20260905T234500Z, order_id
# QITEM-PB-600-LAUF4-FIX-FIRST-VIER-LANES-NACHLAUF-LAUF5-01, Revision 4, Teil A5. Karte
# OA-e10ba2ba39, vom Owner mit Option 1 quittiert ("Anker jetzt anlegen, eigener Commit").
#
# DERSELBE SCHLUESSEL WIE 5.0.0, und das ist eine Entscheidung, keine Bequemlichkeit: die private
# Haelfte liegt seit dem 27.08.2026 in der Owner-Verwahrung am Mac und war noch nie auf einem
# Bauhost. Ein NEUER Schluessel muesste dort erst erzeugt werden — und der Ort, an dem er entsteht,
# ist genau die Frage, um die es bei Selbstbeglaubigung geht. Die oeffentliche Haelfte steht seit
# Commit 71a9fac im Baum (audit_artifacts/pre_tag_trusted_pubkeys.txt) und ist hier NACHGERECHNET:
# kanonisches Base64, 32 dekodierte Bytes.
#
# SIGNIERPOLITIK, eng und ausgeschrieben: dieser Schluessel signiert die drei
# Bereitschaftsartefakte C6.2, C6.3 und C8.2, den Registerkoerper und das Pre-Tag-Receipt von
# 6.0.0, jeweils gebunden an Kandidatencommit, Baumdigest und die beiden Distributionsdigests.
#
# WIE WEIT DIE TRENNUNG HEUTE WIRKLICH REICHT — gemessen am 06.09.2026, nicht behauptet. Eine
# fruehere Fassung dieses Absatzes stand hier: dieser Schluessel signiere KEIN Pre-Tag-Receipt,
# dafuer gebe es den getrennten Anker in pre_tag_trusted_pubkeys.txt, "zwei Vertrauensanker,
# zwei Geltungsbereiche". Der Baum traegt diesen Satz nicht, und er wurde deshalb ersetzt:
#
#   * BEIDE Anker fuehren DENSELBEN Schluessel. iJipntJA8N//h+ln9CgLzeC9n/M5OdCZNeBhbfagom8=
#     steht hier UND in audit_artifacts/pre_tag_trusted_pubkeys.txt. Eine private Haelfte erfuellt
#     beide. Getrennt sind die DATEIEN, nicht die Schluessel.
#   * Genau dieser Schluessel hat die Pre-Tag-Receipts von 5.0.0 und 5.1.0 signiert; `signer_pubkey`
#     in audit_artifacts/500/pre_tag_receipt_v5.0.0.json und audit_artifacts/510/
#     pre_tag_receipt_v5.1.0.json traegt denselben Wert. Der Satz war also schon falsch, als er
#     geschrieben wurde.
#   * Der Pre-Tag-Pfad prueft WEDER Rolle NOCH Frist: `role=` und `not_after=` werden nur auf
#     DIESEM Anker ausgewertet (scripts/audit_candidate_matrix.py), nicht in
#     scripts/pre_tag_receipt_lib.py oder scripts/pre_tag_audit_gate.py. Ein zweiter Schluessel
#     dort wuerde auf dem Papier trennen und in der Mechanik nichts erzwingen.
#
# WAS STATTDESSEN GILT: ein Schluessel deckt beide Anker. Die Trennung der Geltungsbereiche
# besteht heute ueber die Dateien und ueber diese Erklaerung, nicht ueber getrennte Prinzipale.
# Die echte Trennung der Prinzipale — getrennter Schluessel plus Rollen- und Fristpruefung auch
# auf dem Pre-Tag-Pfad — ist Teil E des Auftrags 20260906T092000Z und gehoert nach 6.1, nicht in
# die 6.0.0-Zeremonie: ein Schluessel, der mitten in der Zeremonie entsteht, ist genau die Frage,
# um die es bei Selbstbeglaubigung geht (Owner-Entscheid 06.09.2026, Karte OA-83d26f7eda,
# Option A).
#
# WARUM DIESE ZEILE IN EINEM EIGENEN COMMIT STEHT: `_anchor_last_touched_at_head` weist einen
# Schluessel ab, dessen einzige committete Geschichte der Kandidatencommit selbst ist — wer
# Schluessel und Kandidat in einem Zug einfuehrt, hat sich selbst registriert. Ein eigener Commit
# VOR dem Merge macht ihn zum Vorfahren jedes spaeteren Kandidaten und damit vorab festgelegt.
#
# not_after 2027-09-06: ein Jahr ab heute, Owner-Vorgabe. Geprueft wird gegen ZWEI Fristen (siehe
# das Zeilenformat oben): gegen `produced_at` der Evidenz UND gegen den Tag, an dem das Tor laeuft.
# Am 06.09.2026 aendert die zweite Frist an keinem Verdikt etwas — 2027-09-06 liegt in der Zukunft,
# gemessen ueber alle 33 Pruefungen der Matrix, vorher wie nachher identisch. Sie wirkt ab
# 2027-09-07 und schliesst das Fenster, in dem ein Halter der abgelaufenen privaten Haelfte durch
# Rueckdatierung noch 180 Tage lang Evidenz haette einreichen koennen.
iJipntJA8N//h+ln9CgLzeC9n/M5OdCZNeBhbfagom8= role=readiness_und_register_signierer_600 not_after=2027-09-06
