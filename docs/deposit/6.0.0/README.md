# Deposit package, Technical Note 6.0.0 (DRAFT, prepared before the tag)

**State: nothing deposited, nothing reserved, no Zenodo API call.** This folder is the package for
Deposit B of the 6.0.0 line, assembled on 2026-09-05 on the frozen release head
`049b3195def2734fe69a4ab33b64937e404dc250` so that the deposit can be presented to the owner with the
finished package attached once the tag `v6.0.0` and the release run exist. The deposit itself, the
DOI reservation and the publish step are owner doors and happen after the tag, never before.

## Deposit mode (Depositblatt B pattern of the 5.0.0 deposit)

- **New version** at the existing record *Evidence Integrity for AI Evaluations*, concept DOI
  `10.5281/zenodo.21230466`. Never a new record, never an edit of an old version.
- Previous published version: **5.0.0**, version DOI `10.5281/zenodo.22209091`, 2026-08-31. The
  software releases 5.1.0 and 5.1.0.post1 shipped without a Note revision.
- Metadata in deposit-API form: `zenodo_metadata_note_600.json`. Title, author (name as in
  `CITATION.cff`, ORCID `0009-0006-8947-6065`, affiliation b7n0de, no ROR), resource type
  Publication / Report, license CC-BY-4.0 for the document (the code stays MIT), keywords: unchanged
  against 5.0.0. Changed: version `6.0.0`, the description (one sentence added, deliberately not
  character-identical to 5.0.0), and the related identifiers (tag, PyPI page and software record of
  v6.0.0, plus the concept DOI and the 5.0.0 version DOI named by the owner). Publication date is
  the deposit day.
- The file `zenodo_metadata_note_600.json` must not be renamed to `.zenodo.json` at the repository
  root; that would silence `CITATION.cff` for the software record.

## The set

| File | Role | State on 2026-09-05 |
|---|---|---|
| `EU_Technical_Note_6.0.0.md` | the source of the Note | draft; every value that only a shipped wheel or the tag can produce is marked NOT MEASURED |
| `proofbundle_technical_note_6.0.0_reference.pdf` | the rendered Note, built ONLY with `scripts/b7_technote_render.py` of the operator repository (DejaVu Serif, ligatures off, tagged, A4) | rendered from the draft, 17 pages; not committed here, rebuilt at the tag; digest in `SHA256SUMS_deposit_600.txt` is provisional |
| `example/appendix_claim.json` | the eval claim of Section 3.1 | final (declared example timestamp 2026-09-05T06:00:00Z) |
| `example/receipt.json` | the companion receipt of Section 3.1 and Appendix A | provisional: emitted on the wheel built from the frozen head, re-emitted against the shipped wheel |
| `example/issuer.pub`, `example/root.b64` | the receipt's public key and Merkle root | provisional, change with the receipt |
| `example/note_policy.json` | the instantiated strict policy of Section 3.3 | provisional, pins the receipt's key and root |
| `example/gate_policy_example.json` | the gate policy the decision binds by digest | final (sha256 `6c8a2e17…1bae1b`, unchanged since 5.0.0) |
| `example/decision_predicate.json`, `example/decision_receipt.json` | Section 3.4 | provisional |
| `example/outcome_predicate.json`, `example/outcome_receipt.json` | Section 3.5 | provisional |
| `example/independent_check.py` | the proofbundle-free one-leaf checker of Section 3.7 | final, unchanged since 5.0.0 |
| `example/agent_review_v02_check.py` | the v0.2 verification of Section 3.9 (new in this revision) | final |
| `example/transcript_*.txt` (13 files) | the exact transcripts of Sections 3.1 to 3.7 and 3.9 | provisional, captured on the frozen-head wheel |
| `example/recapture_600.sh` | one command that regenerates every provisional file above against a named wheel | final |
| `zenodo_metadata_note_600.json` | the Zenodo metadata, deposit-API form | draft with `{{...}}` placeholders for post-tag values |
| `SHA256SUMS_deposit_600.txt` | the file list with digests | provisional until the re-capture against the shipped wheel |

The private keys generated by the re-capture (`*.seed`) are never part of the set.

## What was measured on the frozen head, and what was not

Measured on 2026-09-05 (commands and results in the operator's lane report): 2,933 test functions in
230 files; the full run 3,236 passed, 22 skipped (enumerated), 9 deselected, exit 0; 88 mutation
operators, canonical run `OK (88 operators, 0 gap(s))` on `658ed063` with `src/`, `tests/` and
`scripts/` byte-identical to the frozen head; 107 conformance cases in 6 kinds; 8 published
agent-review receipts; 57 public names; 67 source files, 24,283 lines; reproducible sdist at the
head's epoch (`08db75e4…5101c`), wheel `ba4048d6…e20d09`; every transcript of Section 3 re-run
against that wheel in a fresh virtual environment with the exit codes of the 5.0.0 revision
(verify 0, pinned 0, policy 3, decision 0, outcome 0, demo 0, independent check 0).

Not measured, marked in the Note: the tag commit, the release date, the three shipped digests, the
PyPI attestation check, the version DOI, the Internet-Draft revisions of reference 15, and the
regulatory re-check of Section 1.

## After the tag (in this order)

1. Re-capture with the shipped wheel: `PB_VENV=… PB_REPO=… bash example/recapture_600.sh <outdir>`,
   copy the outputs over `example/`, insert the shipped digests and the tag commit into the Note,
   replace every NOT MEASURED that has become measurable.
2. Rebuild the PDF with `scripts/b7_technote_render.py`, check it with the fidelity doctor's
   invariants (fonts, ligatures, tagged, A4, content), regenerate `SHA256SUMS_deposit_600.txt`.
3. Depositblatt B for 6.0.0 (field by field, with the measured preconditions), then the owner card
   with this package attached. Reserve the version DOI only inside that deposit, insert it into
   the Note, publish under the owner's GO.
4. Only then: `CITATION.cff` (`doi:` line of the Note reference) and the README citation line to
   the new version DOI, in a follow-up on `main`.
