# Release scope — 6.2.0 (in progress)

## The measure

A line belongs in 6.2.0 when it brings one of the four outcomes closer, or closes a P1 from the
audit of 2026-09-19, or closes a place where the verifier says more than it checks, or is a
precondition of one of those. Everything else waits. The four outcomes are E1 a second
implementation, E2 in-toto PR 575, E3 three external users, E4 adoption into a specification.

This file replaces the cast of 2026-09-19, which moved 54 lines here unchanged because the cut had
to happen faster than the reading. The lines below were read against the code and the register
before they were placed. Where a finding disagreed with the template, the line stayed and the
disagreement is written down rather than resolved silently.

THE FIRST VERSION OF THIS RECAST DID NOT KEEP THAT PROMISE, and the correction belongs here rather
than in a commit message. The paragraph above claimed the register had been read, while four
entries that `RESTRISIKO_610.md` targets at 6.2.0 BY NAME appeared in neither file. A
counter-reading measured that by searching for the four register keys; all four were absent. They
are the `R-B` rows below. A sentence claiming a reading that did not happen is the same defect this
file exists against, one level up: a statement broader than the work behind it.

## E1 — a second implementation and the interop core

| Item | What it is |
|---|---|
| P29 | Conformance contract. It is BUILT and it is ON `main`: 130 cases in 7 areas (`agent_review` 35, `relation` 45, `cap1` 15, `bundle` 12, `envelope_profile` 10, `provenance` 10, `decision` 3), each a directory with `case.json` and its fixtures, harness `conformance/run_conformance.py`, offline. Measured on `main` 2026-09-23 with the `[anchors]` extra: 130 cases, 130 fully checked, 0 partially checked, 0 not run, 0 failed. What remains under this heading is therefore NOT building or landing the corpus; it is whatever the contract asks beyond it, and that is not measured here. |
| P30 | Exchange attempt with a foreign producer and consumer. TWO candidates, and the second is not a replacement. First, reciprocity for `eval-result/v0.1`: we hand over the corpus with expected verdicts, the other side runs it and reports where their verdict differs, and the same in the other direction — symmetric, which is what this line asks for. Second, the offline profile for the governance receipts of a foreign producer (`src/proofbundle/adapters/agt_receipt.py`, 32 cases over five real receipts, no foreign code copied), which already produced the kind of finding this line wants: their canonical form is `json.dumps(sort_keys=True, separators=(",",":"))` and NOT RFC 8785, which a second implementer would otherwise meet as a silent mismatch. It is the faster route to a measured result and the weaker evidence of reciprocity, because so far only we read their side. Both approaches are outreach and stay an owner door. |
| Z.278 | `classify_eval_claim` returns `invalid` for a foreign bundle identifier, where the honest answer is that the claim is not ours to classify. |
| S26 | `contentRootAlg`: a present but unregistered value falls silently back to LEGACY instead of being refused. A value nobody registered is not a legacy value, it is an unknown one. |
| S32 | `errorContains` checks only the Python side while the corpus reads as though it checked both. |
| S106 / S108 | Rust taxonomy: an empty signature list is classified two ways across the implementations, and five `Err(_)` discards in `main.rs` name no reason. |
| R1 | `scripts/rust_parity_registry.json` SHIPS IN THE SDIST and describes behaviour the code no longer has: it presents `policy_decision: null` as neutral while the verifier emits `POLICY_NOT_EVALUATED` with `safeForAutomation: false`. `RESTRISIKO_600.md:47-60` states the funnel in one sentence — it can reach a reader building a second implementation — and that reader is E1. This line stood in 6.3.0 as "a text correction in our own registry" until a counter-reading measured the shipping path; a document a second implementer reads is not internal text, and the smallest reproduction is a foreign implementation following the registry and assigning a different automation verdict to the same bytes. |

## E4 — a specification adopts something measured

| Item | What it is |
|---|---|
| Z146 | The header sits outside the publisher's control. Per owner word, without B3 only the OTS path is in scope here; a trust anchor per key is NOT a 6.2.0 item. |
| B-2 | Register form after the VEX model, object classes first. |
| B-3 | Three register lines: time anchor, provenance anchor log of the foreign contribution, model seal. |
| B-8 | Register line `FORTSETZUNG_WIDERLEGT`, and the class "the tool says more than it checks" that it records. |

## Trust in the verifier

| Item | What it is |
|---|---|
| Z.715 / Z.728 | A reinterpretation on a pull request covers `absent`, not `rejected`, and the reach of that is not stated in the guard's header. |
| S20 | A gate whose red lines the candidate itself may explain away. |
| S33 | The completeness check of the budget evidence compares itself with itself. |
| S22 | The raw matrix carries 39 and the signed differential 42, and nothing compares the two files. |
| R-A1 | The trust anchor lives in `audit_artifacts/`, the one directory the digest excludes. |
| R-A3 | Evidence paths hardwired to `audit_artifacts/360` instead of derived from the version. |
| S76 | The `GateMetaTest` runs from no shipped sdist. |
| N18 | `pip install <sdist> && pytest` without the `[test]` extras is RED rather than degrading to clean skips. MEASURED 2026-09-23, see below. |
| R-B1 | `schemas/eval_claim_v0_1.schema.json` documents `^sha256:[0-9a-f]{64}$` for `model_id_commit` and `dataset_id_commit`, and the verify boundary does not enforce it: a signed claim with an arbitrary string in either field decodes. Register entry `COMMIT-PATTERN-DOMAIN-NOT-AT-VERIFY-BOUNDARY-01`, `RESTRISIKO_610.md:91-108`. The cost is measured there rather than guessed: the check is three lines, and it turns four of five existing cases in `tests/test_cli_eval.py` red because they sign placeholder commitments. |
| R-B2 | A small-order key at the carrier's signature block verifies, so a carrier carrying a zero public key is shown TO A REVIEWER as signed in `audit_artifacts/600/views/uebersicht.md`. Register entry `SMALL-ORDER-KEY-AT-CARRIER-SIGNATURE-01`, `RESTRISIKO_610.md:116-141`. `_signaturzeile` and `pruefe_v2` share one exit by explicit design, so this is one fix and not two. |
| R-B3 | `pyproject.toml` states that eight mypy versions and six ruff versions exit 0 over this tree, and `tests/test_shipped_comment_numbers.py` binds the SIZE of each set without re-running either tool. Register entry `SHIPPED-TOOL-VERDICT-NOT-RE-RUN-01`, `RESTRISIKO_610.md:143-148`. It is the house class "the tool says more than it checks", written on our own manifest. |
| R-B4 | Three public exporters in `src/proofbundle/intoto.py` (`:246`, `:424`, `:551`) coerce `passed`, so a correctly signed but genuinely failing claim carrying the string `"false"` yields `PASSED`, `passed: true` and the threshold-met property to a library caller who never passes `decode_eval_claim`. Register entry `DREI-VERBRAUCHER-COERCEN-PASSED-DOKUMENTIERT-IST-EINER-01`, `RESTRISIKO_610.md:150-185`, which asks for a CLASS fix at the public functions rather than three guards. |

### N18, measured

A bare `pip install proofbundle-6.1.0.tar.gz` followed by `pytest` in the extracted sdist exits 2,
a collection error, and the run is interrupted after 19 collected tests:

```
ERROR tests/test_action_input_injection.py
tests/test_action_input_injection.py:21: in <module>
    import yaml
E   ModuleNotFoundError: No module named 'yaml'
```

It is one file and one line. Of the six test modules that import an optional dependency, five guard
it with `pytest.importorskip` and this one does not. The invariant the CI step claims to pin is the
one it does not exercise: `published-artifact-gate.yml` installs the `[test]` extra and only then
runs pytest, so the bare case never executes there.

## Conditional

| Item | What it is |
|---|---|
| C1 / N15 | sdist reproducibility. The build half is MEASURED GREEN and the line is narrowed to what remains. |

### C1, measured, and the line as written is no longer true

Built with `scripts/build_reproducible.py` from the git tree at `v6.1.0` (`dcac5aee`),
`SOURCE_DATE_EPOCH` from the commit time, which is the one line `release.yml` runs:

```
proofbundle-6.1.0-py3-none-any.whl   published f43164161952d78a…   rebuilt f43164161952d78a…   IDENTICAL
proofbundle-6.1.0.tar.gz             published d63554915940ce42…   rebuilt d63554915940ce42…   IDENTICAL
two runs of the same tree: identical for both artefacts
```

The line reads "sdist bit-reproducible, check or change the build backend". Both delivered
artefacts reproduce bit for bit on the shipped path, so **changing the backend would be the wrong
work**. What remains under this heading is S27, the `egg-info` cache: rebuilding an sdist FROM an
sdist is not a fixed point, measured as one line of difference in
`src/proofbundle.egg-info/SOURCES.txt`, where the rebuild lists `setup.cfg` and the original does
not. The line stays here rather than being deleted, because its subject is not empty; it is smaller
than it says.

## Accounting

Counted from the rows of the tables above, not asserted.

| | Count |
|---|---|
| Rows cast here | 24 |
| of which new from the four outcomes | 3: P29, P30, Z146 |
| of which new from the register of 6.1.0 | 4: R-B1, R-B2, R-B3, R-B4 |
| identifiers of the 54 of 2026-09-19 carried here | **18** |
| Moved to 6.3.0 | 35 identifiers in 34 rows, see `docs/release_scope/6.3.0.md` |
| Without a row of its own | 1: S62 / S65-3, done in 6.1.0 |
| Sum against the 54 | 18 + 35 + 1 = **54** |

Rows and identifiers are not the same number, and that is not an error: `C1 / N15` here and
`B1 / N14` in 6.3.0 each carry two identifiers in one row, exactly as the source list of
2026-09-19 wrote them. N14 and N15 also appear as bookkeeping, because their register target is
6.1 and they leave the scope with a sentence; that is a statement about the register, not a
third placement.

WHAT THE 18 COUNTS, because the unit is where the first calculation of this table went wrong. It
counts identifiers of the LIST IN `RESTRISIKO_610.md:31`, which names the 54 with one identifier
per line. So `C1 / N15` contributes two, because the list carries both, while `S106 / S108` and
`Z.715 / Z.728` contribute one each, because the list carries only the leading one. Counting the
identifiers printed in this file instead gives 20 and would close against nothing.

**R1 and N3-5 traded files after a counter-reading, and the 18 did not move.** Both are in that
list of 54, so one leaving and one arriving keeps the number. That is the arithmetic working as
intended rather than a coincidence: the sum is a statement about WHERE the 54 are, not about how
many rows each file happens to have.

## The cut rule

6.2.0 is cut as soon as E1 stands, with no date. E1 stands when P29 has landed and P30 shows a
foreign producer or consumer with a MEASURED result, green or red with a finding, not announced.

## What does not move here

The Out list of 6.1.0 stays where it is. What was excluded there with a reason is not reopened by
this cast; taking one of those lines back needs an owner word.
