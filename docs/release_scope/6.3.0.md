# Release scope — 6.3.0 (not started)

## Why these lines are here and not in 6.2.0

6.2.0 is cut against the four outcomes: E1 a second implementation, E2 in-toto PR 575, E3 three
external users, E4 adoption into a specification. A line belongs there when it brings one of them
closer, closes a P1 of the audit of 2026-09-19, or closes a place where the verifier says more than
it checks.

The lines below do none of those. Each one carries the sentence saying so, as the release rule of
2026-09-09 requires: a register line per move, not a bulk transfer. They are open, not dismissed;
`RESTRISIKO_610.md` names them and continues to.

## Mutation gate and coverage

| Item | Why it carries no outward outcome |
|---|---|
| A1 | The mutation collector sees 2537 of 3702 units. It improves our confidence in our own suite; no external party reads that number. |
| A2 | Coverage check of the operator set per directory. Same shape as A1, one level down. |
| A3 | The real mutation gaps of part D, each with a catch proof. Internal assurance work. |
| N17 | The parity gate rules from the ABSENCE of complaints. It is a gate over our own corpus and changes no interface. |
| N20 | One mutation operator is NOT MEASURABLE at 111 GiB resident. A resource limit in our own harness. |
| R-A2 | `C4.1` / `C4.2` rule without the `population_complete` bound. Belongs with N17 and shares its reason. |

## Guards and ledger

| Item | Why it carries no outward outcome |
|---|---|
| A5.1–A5.4 | Guard inventory, own catch proof, stocktaking, class in the ledger. Bookkeeping about our own guards. |
| S5 | The two new guards are never bound TOGETHER. An internal combination nobody outside runs. |
| S29 | The throwaway tree of the number binding produces its own precondition. Harness hygiene. |
| S30 | Three named limits of the import guard. Internal guard surface. |
| S31 | The corpus generator guard covers `agent_review`, not `relation`. Coverage of our own generator. |
| S59 / S60 / S65 | Tree guard of the gate meta test and its class neighbour in path form. Internal. |
| S64 / S65-2 | Two rejected ledger entries. Bookkeeping. |
| N1-2a–2c | Neighbour sweep of the transition state, meta test, ledger entry. Internal class work. |

## Measurement run and counting unit

| Item | Why it carries no outward outcome |
|---|---|
| N2-1 | Schema change of the measurement-run binding. Changes how we record our own runs. |
| N2-2 | Anti-parity as an admission condition with a counter-example. Internal gate condition. |
| N2-3a–d, 3f, 3g | Freeze the counting unit, split the day class, ladder instead of switch, 42 difference lines, compute the metric. Our own metric. |
| N1-1a | Name the soak box. Ends in a PROPOSAL, not an order. |
| N1-1c | Requirement: claim no 24-hour soak anywhere without 24-hour evidence. A rule about our own wording, and it binds already. |

## Signature and tree

| Item | Why it carries no outward outcome |
|---|---|
| N3-1 | The evidence leaves the examined tree. Ordering point 1 of that chain, internal. |
| N3-2 | Bind commit and tree together. Ordering point 2. |
| N3-3 | Per signature: purpose, semantics, recipient, duration, dependencies. A written statement about our own keys. |
| N3-4 | Freeze note: what it binds, what is missing for a closed chain. Follows N3-5, which IS in 6.2.0 because it is a measurement of the published artefacts. |
| B1 / N14 | Close N14: `pre_tag_receipt.py` and `gen_findings_register.py` on emit/assemble. Internal tooling path. |

## The signature block, per owner word

| Item | Why it carries no outward outcome |
|---|---|
| B2 | Separate signing principal on the Farmer. Owner word of 2026-09-23: OUT, after 6.3.0. Key custody, not an outward result. |
| B3 | Trust anchor with role, validity and signing policy per key. Same owner word, same reason. Z146 in 6.2.0 explicitly takes the OTS path without it. |

## sdist

| Item | Why it carries no outward outcome |
|---|---|
| C2 | Carry the release note forward once C1 stands. Follows C1 and cannot precede it. |
| S27 | The sdist build carries an `egg-info` cache that survives a deleted `MANIFEST.in` line. MEASURED 2026-09-23 as one line of difference in `SOURCES.txt` when an sdist is rebuilt FROM an sdist. It is real and it is small; the delivered artefacts reproduce bit for bit on the shipped path, so nothing outward depends on it. |

## Register remainders

| Item | Why it carries no outward outcome |
|---|---|
| B-7 | B25, operator groups and failure domains in the witness policy. A policy detail no external party reads yet. |
| B-9 | Cedulon adapter package B, template D1 cross-check. Cedulon is a customer and belongs in the work36 channel, not in this scope. |

## Cleaning up

| Item | Why it carries no outward outcome |
|---|---|
| S65-5 | Delete the three superseded local branches. Housekeeping. |
| R1 | `rust_parity_registry.json` describes behaviour the code no longer has. A text correction in our own registry. |
| R2 | One subfield reads safer than before, NOT re-measured. A measurement we owe ourselves. |
| R3 / R4 | `resolve_receipt_chain` raises raw on a non-mapping, `or []` in 19 places. Robustness of our own verifier internals; the never-raise promise at the public surface is checked separately. |

## Accounting

Counted from the rows of the tables above, not asserted.

| | Count |
|---|---|
| Rows moved here | **34**, carrying **35** of the 54 identifiers |
| Rows cast into 6.2.0 | 20, carrying 18 of the 54 plus 3 new ones |
| Without a row of its own | 1: S62 / S65-3, done in 6.1.0 |
| Sum against the 54 of 2026-09-19 | 35 + 18 + 1 = **54** |
| Plus the three new lines | 57 |

Rows and identifiers differ by one on each side because `B1 / N14` here and `C1 / N15` in 6.2.0
each carry two identifiers in one row, as the source list wrote them.

**Deviation from the template, stated rather than smoothed.** The order named 31 lines for this
file. Counted by the rows of the quoted tables of 2026-09-19, it is **34**. The arithmetic closes
against 54 either way, so the difference is in how grouped rows are counted, not in a lost line:
`A5.1–A5.4`, `S59/S60/S65`, `N1-2a–2c`, `N2-3a–d` and `R3/R4` are ONE row each in the source list
and are kept as one row here. No line was dropped, and none was invented.
